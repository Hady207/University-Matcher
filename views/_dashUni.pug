

mixin universitiesLis(i,img,name,uniId)
    li.dashboard__li
        .user__main__list
            img.user__profile(src=`/img/${img}`)
        
        h2.user__name=name
        .detail__button__div
            a.b1.b1__edit.b1__delete--sm.details__buttons(id=`details__button-${i}` data-finduni=uniId) Details


mixin reviewCard(review,img,name,createdAt,rating)
    figure.review
        blockquote.review__text=review
        figcaption.review__user
            img.review__photo(src=`img/users/${img}`)
            .review__user-box
                p.review__user-name=name
                p.review__user-date=createdAt
            .review__raitng=rating

mixin reviewsLis(uniId,title,review,img,name,createdAt,rating)
    figure.review
        .review__box
            h3.reviewview__title=title
            a.b1.b1__delete--sm(href="") Delete

        blockquote.review__text=review
        figcaption.review__user
            img.review__photo(src=`img/users/${img}`)
            .review__user-box
                p.review__user-name=name
                p.review__user-date=createdAt
            .review__rating=rating


mixin userLis(img,name,userId)
    li.dashboard__li
        .user__main__list
            img.user__profile(src=`/img/users/${img}`)
        
        h2.user__name=name
        .detail__button__div
            a.b1.b1__delete--sm(href=`/${userId}`) Details




  
         
.content
    nav.side__nav
        ul.side-nav
            li.side-nav__item.side-nav__item--active(id="menu_university")
                a.side-nav__link(id="universities" href="#")
                    ion-icon(name="school")
                    span Universities
            
            li.side-nav__item(id="menu_review")
                a.side-nav__link(id="reviews" href="#")
                    ion-icon(name="paper")
                    span Reviews

            li.side-nav__item(id="menu_user")
                a.side-nav__link(id="users" href="#")
                    ion-icon(name="contacts")
                    span Users
        
        .legal &copy; 2020 All rights reserved
    
    main.dashboard__universitiesview(id="universities__main" style="display:block;")
        .dashboard__container
            ul.dashboard__ul
                each fu,i in user.favoriteUni
                    +universitiesLis(i,fu.coverImage,fu.name,fu.id)
            
            
    main.dashboard-view(id="university__main" style="display:none;")
       
                    
            //- .dashboard__reviews
            //-     +reviewCard("loremloreoreolroerloreloeroreo","leo","leo","23/3/2019","4.5")
                    

            


    .edit__modal(id="edit__modal")
        .info-x
            a.symbol-x(id="exit" href="#") &#10008;
        
        form.form__edit(action="", method="post")
            .modal__card
                .form__group
                    label.form__label(for="name") University Name:
                    input.form__input(id="name",name="name",type="text")

                .form__group
                    label.form__label(for="abbrv") abbreviation:
                    input.form__input(id="abbrv",name="abbrv",type="text")

                .form__group
                    label.form__label(for="email") email:
                    input.form__input(id="email",name="email",type="email")
                
                .form__group
                    label.form__label(for="website") website:
                    input.form__input(id="website",name="website",type="text")
                
                .form__group
                    label.form__label(for="address") address:
                    input.form__input(id="address",name="address",type="text")
                
                .form__group
                    label.form__label(for="desc") description:
                    textarea(id="desc",name="description",cols="30",rows="10")

            button.b1.b1__edit--sm(style="margin-top: 2rem;") Submit   
            
    main.dashboard__reviewview(id="review__main")
        .dashboard__reviews
            +reviewsLis("welqowe","Taats weird","loerlorelore","aarav","aarav","23/43/2012","8")
            

    main.dashboard__userview(id="user__main")
        .dashboard__container
            ul.dashboard__ul
                +userLis('aarav','John','122o102o0')
              
                            

block scripts
    script(src="./js/UI/dashboard.js")
    //- script(src="./js/UI/dashUni.js")